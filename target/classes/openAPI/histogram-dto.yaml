components:
  schemas:
    HistogramDTO:
      properties:
        name:
          type: string
          description: "The name of the parameter (for example, city, brand, etc.)"
        count:
          type: integer
          format: int64
          description: "The number of hotels associated with the parameter"
      required:
        - name
        - count
